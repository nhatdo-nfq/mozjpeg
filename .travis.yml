language: c
os: linux
dist: bionic
before_install:
    - sudo apt-get update -y && sudo apt-get install -y cmake zlib1g-dev libpng-dev git nasm
compiler:
    - clang
    - gcc
script: cmake -G"Unix Makefiles" . && make SHARED=0 CC='gcc -static' && make install

